# Base URL of the FloodCast backend.
# Local development points at the dev server; production points at the
# deployed Render service. Never hardcode this in source.
VITE_API_BASE_URL=http://localhost:8000
