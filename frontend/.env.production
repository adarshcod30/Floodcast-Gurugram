# Base URL of the FloodCast backend, baked in by Vite at build time.
# Not a secret — it ends up in the client bundle regardless — so it is
# committed rather than configured per-platform. See .env.example for
# the local-development counterpart.
VITE_API_BASE_URL=https://floodcast-gurugram-api.onrender.com
