# ── Web framework ───────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# ── Data access ─────────────────────────────────────────────────────
# DuckDB queries the Parquet files in-process — no database service to
# run, provision, or pay for on a free tier.
duckdb==1.2.2
pandas==2.2.3
pyarrow==19.0.0

# ── Weather providers + caching ─────────────────────────────────────
# Open-Meteo (primary) needs no API key; OpenWeatherMap is optional.
httpx==0.28.1
cachetools==5.5.1

# ── LLM: Claude on AWS Bedrock ──────────────────────────────────────
# The Bedrock Mantle client speaks the Messages API, so model IDs and
# request shape match the first-party API. Entirely optional at runtime:
# with no AWS credentials the chat pipeline runs its rule-based path.
anthropic[bedrock]==0.120.2
boto3==1.36.14

# ── Agent orchestration ─────────────────────────────────────────────
langgraph==0.2.73
langchain-core==0.3.34

# ── Operational guardrails ──────────────────────────────────────────
slowapi==0.1.9

# ── Geocoding (place-name resolution fallback) ──────────────────────
geopy==2.4.1

# ── Testing & linting ───────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.25.3
ruff==0.9.6
